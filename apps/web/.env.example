# ═══════════════════════════════════════════════════════════
# Firebase Configuration - Web App
# Sistema de Riego Inteligente - Uniminuto
# ═══════════════════════════════════════════════════════════
#
# ⚠️  IMPORTANTE: Este es un archivo de EJEMPLO
# 
# 1. Copia este archivo como `.env` en la raíz de apps/web/
# 2. Reemplaza los valores placeholder con tus credenciales reales
# 3. NUNCA subas el archivo .env a Git (está en .gitignore)
#
# ═══════════════════════════════════════════════════════════

# ─────────────────────────────────────────────────────────
# Firebase SDK Configuration
# ─────────────────────────────────────────────────────────
# Obtén estos valores desde:
# Firebase Console → Project Settings → General → Your apps

NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project-id.firebasestorage.app
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id

# ─────────────────────────────────────────────────────────
# Firebase Cloud Messaging (FCM)
# ─────────────────────────────────────────────────────────
# Obtén este valor desde:
# Firebase Console → Project Settings → Cloud Messaging → Web Push certificates

NEXT_PUBLIC_FIREBASE_VAPID_KEY=your_vapid_key_here

# ─────────────────────────────────────────────────────────
# Ejemplo con valores ficticios (para referencia)
# ─────────────────────────────────────────────────────────
# NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSyABC123DEF456GHI789JKL012MNO345PQR
# NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=mi-proyecto.firebaseapp.com
# NEXT_PUBLIC_FIREBASE_PROJECT_ID=mi-proyecto
# NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=mi-proyecto.firebasestorage.app
# NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789012
# NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789012:web:abc123def456
# NEXT_PUBLIC_FIREBASE_VAPID_KEY=BJAbc123Def456Ghi789Jkl012Mno345Pqr678

# ─────────────────────────────────────────────────────────
# Notas de Seguridad
# ─────────────────────────────────────────────────────────
#
# ✅ Es SEGURO exponer estos valores en el cliente (NEXT_PUBLIC_*)
#    porque Firebase usa reglas de seguridad en el backend
#
# ⚠️  NUNCA incluyas claves privadas o service account keys aquí
#
# 🔒 Asegúrate de configurar Firebase Security Rules correctamente:
#    - Firestore Rules en Firebase Console
#    - Storage Rules si usas Firebase Storage
#    - Authentication settings
#
# ─────────────────────────────────────────────────────────
# Troubleshooting
# ─────────────────────────────────────────────────────────
#
# Si las variables no se cargan:
# 1. Verifica que el archivo se llame exactamente `.env` (no .env.txt)
# 2. Reinicia el servidor de desarrollo: `bun run dev`
# 3. Limpia la caché: `rm -rf .next && bun run dev`
#
# Si FCM no funciona:
# 1. Verifica el VAPID key en Firebase Console
# 2. Verifica que el Service Worker esté registrado
# 3. Revisa permisos de notificaciones en el navegador
#
# ─────────────────────────────────────────────────────────
